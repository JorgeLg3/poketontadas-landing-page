---
import { Image, Picture } from "astro:assets";
import GameboxImg from "../assets/imgs/gamebox.png"
import Button from "../components/Button.astro";
---
<section id="download" class="section section-dark">
    <h2 class="title">Disponible Próximamente</h2>
    <div  class="content-section">
        <div class="image-row">
            <div class="image-container">
                <Picture class:list={"image"} src={GameboxImg} alt="Characters Img" height={300}/>
            </div>
        </div>
        <div class="text-row">
            <div class="text-container">
                <p>¡Descarga ya el juego y comienza tu aventura!</p>
                <p>
                    El mundo de los Pokémon te espera: 
                    entrena a tu equipo, recorre la Región de Tontaria y disfruta de las nuevas funcionalidades multijugador.
                </p>
                <Button title="Descarga" class="download-button"/>
                <span class="alert">Lanzamiento el 10/01/25</span>
            </div>
        </div>
    </div>
</section>
<style>
    .title{
        text-align: center;
        margin-top: 2rem;
    }

    .section{
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    .content-section {
        max-width: 960px;
        display: flex;
        flex-direction: column;
        padding: 2rem 1rem;
        align-items: center;
        justify-content: center;
        gap: 1.5rem
    }
    .section-dark {
        background-color: var(--lightblue);
        color: var(--darkblue);
    }

    .section-light {
        background-color: var(--white);
        color: var(--darkblue);
    }

    .text-row{
        padding: 0rem;
    }

    .text-container {
        color: var(--darkblue);
        background-color: var(--white);
        padding: 2rem;
        border-radius: 3px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.2);
    }

    .text-container h2 {
        
    }

    .text-container p {
        margin-top: 1rem;
    }

    .image-row{
    }

    .image-container{
        /* background-color: yellow; */
        display: flex;
        justify-content: center;
    }

    .image{
        border-radius: 5px;
        margin-left: auto;
        margin-right: auto;
    }

    .content .text-row h2 {
        margin-bottom: 1rem;
    }

    .download-button{
        margin-top: 2rem;
        background-color: var(--accent);
        filter: blur(2px);
        position: relative;
        box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.2);
    }

    .alert{
        position: relative;
        margin-top: -30px;
        font-weight: 600;
    }

    .download-button:hover {
        /* background-color: var(--accent2); */
        background-color: var(--accent);
        cursor:default;
    }

    /* Responsive Design - full screen */
    @media (min-width: 768px) {
        .content-section {
            flex-direction: row-reverse;
            display: flex;
            gap: 16px;
            padding: 3rem 2rem;
        }

        .text-row{
            flex-basis: 50%;
        }

        .image-row{
            flex-basis: 50%;
            margin-top: 0;
        }

        .download-button, .alert{
            align-self: flex-end;
            margin-right: -70px;
        }
    }
</style>